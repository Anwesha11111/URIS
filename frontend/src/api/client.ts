/**
 * Re-export the centralized API client from the services layer.
 * All new code should import from `../services/api` directly.
 * This file is kept for backward compatibility only.
 */
export { default } from '../services/api'
